
<div id="bread-crumbs">
  <ul>
    <li><h3>Categories</h3></li>
  </ul>
</div>
<div id="list">
    <ul>
    <% @categories.each do |category| %>

        <li>
          <h2><%= link_to(category.category, {:controller => 'public', :action => 'subcategories', :id => category.id}) %></h2>

        </li>
    <% end %>
    </ul>
</div>